"""Custom widgets for textual_ui applications.

Add shared/custom Textual widgets here. Each widget should be a file
for clarity, re-exported through this module's namespace.

Example:
    ```python
    from textual_ui.widgets import MyWidget

    class MyApp(TextualApp):
        def compose(self) -> ComposeResult:
            yield MyWidget()
    ```
"""
