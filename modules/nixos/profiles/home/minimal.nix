# modules/nixos/profiles/home/minimal.nix
# Minimal home profile - imported by default.nix
{ ... }:
{
  # Minimal profile settings are handled in default.nix
  # This file exists for future expansion
}
