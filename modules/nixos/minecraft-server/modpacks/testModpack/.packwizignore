# Nix-side build artifacts — never shipped to players, keep out of the index.
checksums.json
# Nix-side patch machinery — same treatment (packwiz refresh indexes these otherwise).
patches.nix
patches/
