# Core Home Configuration

Git, Nix settings, and agenix secrets for home-manager.

## Included Modules

- `git.nix` — Git configuration
- `nix.nix` — Nix daemon settings
- `agenix.nix` — User secrets via agenix
